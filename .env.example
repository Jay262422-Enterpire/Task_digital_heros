# Local SQLite (this checkout). Prisma `provider` is still `sqlite`.
# For a new Supabase Postgres project, paste the URIs below and send them
# back — do not point this app at Postgres until we switch the schema.
DATABASE_URL=""
DIRECT_URL=""

# JWT / session-cookie signing for `dh_session`. Not a Stripe or Supabase key.
# Generate: openssl rand -base64 32
AUTH_SECRET=""

# Stripe — checkout is still mocked. Create these in the Dashboard and paste
# them back; we wire checkout/webhooks only after that.
STRIPE_SECRET_KEY=""
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=""
STRIPE_WEBHOOK_SECRET=""
STRIPE_PRICE_MONTHLY=""
STRIPE_PRICE_YEARLY=""
